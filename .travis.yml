language: ruby
cache: bundler
before_script: "./bin/ci/before_build"
script: "bundle exec rspec"

notifications:
  recipients:
    - michael@rabbitmq.com

services:
  - rabbitmq

branches:
  only:
    - master
